package <%=packageName%>.service.util;

import javax.json.bind.Jsonb;

public final class JsonbTransformationUtil {

    /**
    * Трансформация строки в объект
    */
    public static Object fromJson(Jsonb jsonb, String data, Class objectClass) {
        return jsonb.fromJson(data, objectClass);
    }
}
